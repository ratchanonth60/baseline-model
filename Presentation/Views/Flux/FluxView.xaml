<UserControl x:Class="BaselineMode.WPF.Views.Flux.FluxView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:scott="clr-namespace:ScottPlot;assembly=ScottPlot.WPF"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1200"
             Background="#1E1E1E">
    <UserControl.Resources>
        <Style x:Key="ChartCard" TargetType="Border">
            <Setter Property="Background" Value="#252526"/>
            <Setter Property="BorderBrush" Value="#3E3E42"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="4"/>
            <Setter Property="Padding" Value="8"/>
        </Style>

        <DataTemplate x:Key="FluxPlotItemTemplate">
            <Border Style="{StaticResource ChartCard}" Margin="4">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <TextBlock Text="{Binding LayerName}" Foreground="#26C6DA" FontSize="12" FontWeight="Bold" Margin="4,2"/>
                    <scott:WpfPlot Grid.Row="1" MinHeight="180" x:Name="Plot" 
                                   Loaded="Plot_Loaded" 
                                   MouseDoubleClick="Plot_MouseDoubleClick"/>
                    <TextBlock Grid.Row="2" Text="{Binding StatsText}" Foreground="#858585" FontSize="10" Margin="4,2"/>
                </Grid>
            </Border>
        </DataTemplate>
    </UserControl.Resources>

    <Grid Margin="8">
        <ItemsControl ItemsSource="{Binding Layers}" ItemTemplate="{StaticResource FluxPlotItemTemplate}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <UniformGrid Rows="2" Columns="4"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
        </ItemsControl>
    </Grid>
</UserControl>
